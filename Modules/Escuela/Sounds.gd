extends Spatial

var timer = Timer.new()

# Called when the node enters the scene tree for the first time.
func _ready():
#	timer.connect("timeout",self,"do_this")
#	timer.wait_time = 15
#	timer.one_shot = true
#	add_child(timer)
#	timer.start()
	#	$ScaryEffects.play()
	pass

	
